name: Update Matchup Bets CSV
on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  update-csv:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Download latest CSV from Google Sheet
        run: |
          echo "Downloading Matchup Bets sheet as CSV..."
          curl -L -o matchup-bets.csv \
            "https://docs.google.com/spreadsheets/d/e/2PACX-1vRaE2ATXG7IcOEtFQN0oSydGAe2okpyuZ6dTfV_vfbolt2V7CSZzX4FDFnSemJhxcceG8idxPz8v2y5/pub?gid=857449328&single=true&output=csv" \
            -H "Cache-Control: no-cache, no-store, must-revalidate" \
            -H "Pragma: no-cache" \
            || echo "Download failed (curl exit code $?)"
          echo "Downloaded file size:"
          ls -lh matchup-bets.csv || echo "File not created"
          echo "First 5 lines:"
          head -n 5 matchup-bets.csv || echo "File appears empty"
      - name: Commit and push if changed
        run: |
          git config --global user.name "GitHub Action Bot"
          git config --global user.email "actions@github.com"
          git add matchup-bets.csv
          if git commit -m "Auto-update matchup-bets.csv from Google Sheet"; then
            echo "✅ Changes committed, pushing..."
            git push
          else
            echo "ℹ️ No changes to commit (CSV is identical to the version in the repo)"
          fi
